import { PhasePlaceholder } from '@/components/ui/PhasePlaceholder'

export default function SqlLearningPage() {
  return (
    <PhasePlaceholder
      title="SQL Learning"
      phase="Fase 4"
      description="Perguntas de negócio + SQL real + explicação, direto das views de analytics — implementado na Fase 4 (Dashboard/Analytics)."
    />
  )
}
