import { PhasePlaceholder } from '@/components/ui/PhasePlaceholder'

export default function PromptLabPage() {
  return (
    <PhasePlaceholder
      title="Prompt Lab"
      phase="Fase 7"
      description="Comparação lado a lado entre versões de prompt sobre o mesmo input real, com avaliação humana do vencedor — implementado na Fase 7."
    />
  )
}
