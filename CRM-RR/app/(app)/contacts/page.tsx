import { PhasePlaceholder } from '@/components/ui/PhasePlaceholder'

export default function ContactsPage() {
  return (
    <PhasePlaceholder
      title="Contatos"
      phase="Fase 2"
      description="Listagem e CRUD de contatos, ligado ao schema crm.contacts — implementado na Fase 2 (DB + Auth + CRUD básico)."
    />
  )
}
