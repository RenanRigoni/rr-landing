import { PhasePlaceholder } from '@/components/ui/PhasePlaceholder'

export default function CompaniesPage() {
  return (
    <PhasePlaceholder
      title="Empresas"
      phase="Fase 2"
      description="Listagem e CRUD de empresas, ligado ao schema crm.companies — implementado na Fase 2 (DB + Auth + CRUD básico)."
    />
  )
}
